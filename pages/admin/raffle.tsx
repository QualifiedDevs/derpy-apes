import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const raffle = styled((props) => {
    return (
        <Box {...props}>
            Admin Shit!
        </Box>
    );
})``;

export default raffle;
