import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const current = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default current;
