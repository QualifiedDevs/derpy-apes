import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const history = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default history;
