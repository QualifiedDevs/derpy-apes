import {styled} from '@mui/material/styles';
import {Box, Button} from '@mui/material';

const PublicMint = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default PublicMint;
