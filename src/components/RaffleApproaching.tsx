import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const RaffleApproaching = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default RaffleApproaching;
