import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const RaffleResults = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default RaffleResults;
